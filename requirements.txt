# Core dependencies
numpy>=1.26.4  # for gym-pcgrl compatibility
pandas>=2.2.3 # for numpy numpy==1.26.4 compatibility
matplotlib>=3.4.0
pillow>=8.3.0

# Machine Learning / RL
torch>=1.10.0
stable-baselines3>=1.6.0
gym>=0.21.0,<0.26.0  # gym-pcgrl requires old gym
shimmy>=2.0  # Required for stable-baselines3 + gym compatibility

# Resource Monitoring
psutil>=5.8.0
nvidia-ml-py3>=7.352.0  # For GPU monitoring (optional)

# Utilities
tqdm>=4.62.0
scipy>=1.7.0

# Development / Testing (optional)
pytest>=6.2.0
jupyter>=1.0.0
ipykernel>=6.0.0

# Visualization (optional)
seaborn>=0.11.0
wandb>=0.12.0  # For experiment tracking (optional)

# Note: gym-pcgrl must be installed separately
# cd gym-pcgrl && pip install -e .
